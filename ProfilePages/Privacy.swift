import SwiftUI

struct Privacy : View {
    var body: some View{
        VStack{
            Text("🔐Don't worry your data is safe and private").font(.headline)
            Text("until someone offers me some good package 👀🤓 ").font(.subheadline)
        }.padding()
    }
}
